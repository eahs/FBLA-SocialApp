<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FBLASocialApp.Views.AllMembers.AllMembersPage"
             x:Name="allMembersPage"
             Title="AllMembers"
             xmlns:controls="clr-namespace:FBLASocialApp.Controls">

    <ContentPage.Content>
        <StackLayout Padding="0, 16" Spacing="24">
            <controls:SearchableChatList
                x:Name="ListView"
                AllowSwiping="True"
                AutoFitMode="Height"
                FocusBorderThickness="0"
                ItemsSource="{Binding GetAllMembersClicked}"
                TapCommand="{Binding MemberClicked}"
                SelectionGesture="Tap"
                SelectionMode="Single"
                SwipeOffset="156"
                VerticalOptions="FillAndExpand">
                <controls:SearchableChatList.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <Grid
                                    x:Name="Grid"
                                    Padding="16,12"
                                    ColumnSpacing="16"
                                    RowSpacing="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                    </Grid.RowDefinitions>

                                    <!--  Member Profile Name  -->
                                    <Label
                                        Grid.Column="1"
                                        FontFamily="{StaticResource Montserrat-SemiBold}"
                                        FontSize="16"
                                        HorizontalOptions="Start"
                                        LineBreakMode="NoWrap"
                                        LineHeight="{OnPlatform Default=1.5,
                                                                iOS=-1}"
                                        Text="Makenna"
                                        TextColor="{DynamicResource Gray-900}" />
                                </Grid>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </controls:SearchableChatList.ItemTemplate>
            </controls:SearchableChatList>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>